<h1>Public area</h1>

<% if current_user.loan_offers.empty? %>
    <%= link_to 'Apply for a loan', new_loan_offer_path, :class => 'btn btn success' %>
<% else %>
    <%= link_to 'Change loan application', edit_loan_offer_path(:id => current_user.id), :class => 'btn btn success' %>
<% end %>